<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heavendevelopment.mantvida2017">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.DevocionaisMain" />
        <activity android:name=".Activity.ProjetoVidaMain" />
        <activity android:name=".Activity.VisualizarDevocional" />
        <activity android:name=".Activity.CriarDevocional" />
        <activity android:name=".Activity.CriarMetaProjetoVida" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Activity.VisualizarMetaProjetoVida" />
        <activity android:name=".Activity.PlanoLeituraMain" />
        <activity android:name=".Activity.LeituraBiblica" />
        <activity android:name=".Activity.PalavrasMain" />
        <activity android:name=".Activity.VisualizarPalavra" />
        <activity android:name=".Activity.EventosMain" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.VisualizarEventoActivity"
            android:label="@string/title_activity_visualizar_evento"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.ConfiguracoesActivity" />
        <activity
            android:name=".Activity.ProjetoVidaTexto"
            android:label="@string/title_activity_projeto_vida_texto"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>